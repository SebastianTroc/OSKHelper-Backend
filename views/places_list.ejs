<% include header.ejs %>

<h1><%= title %></h1>

<% if (data.places.length) { %>

<table class="table table-hover table-striped table-bordered">
	<thead>
		<tr>
			<th>Zdjęcie</th>
			<th>Nazwa</th>
			<th>Adres</th>
			<th>Zajęty</th>
			<th>Akcje</th>
		</tr>
	</thead>
	<tbody>
		<% data.places.forEach(function(place){ %>
		<tr>
			<td class="center">
				<img class="img-polaroid" width="106" height="106" src="/assets/<%= place.photo %>" alt="Photo">
			</td>
			<td><%= place.name %></td>
			<td><%= place.address %></td>
			<td>
				<% if (place.occupated) { %>
					<a href="/instructors/<%= place.occupated %>"></a>
				<% } else { %>
					<%= place.occupated %>
				<% } %>
			</td>
			<td>
				<a href="/places/<%= place._id %>">Szczegóły </a>
				<a href="/places_edit/<%= place._id %>">Edycja </a>
				<a href="/places_delete/<%= place._id %>">Usuń </a>
			</td>
		</tr>
		<% }) %>
	</tbody>
</table>

<% } else { %>

Jeszcze nie ma żadnych placów w bazie.

<% } %>

<% include footer.ejs %>